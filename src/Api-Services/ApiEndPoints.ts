const baseUrl = 'https://caller-crm-api.justvy.in';

const multiVendor = `${baseUrl}/create_multivendor_users/`;
const customers=`${baseUrl}/customers/`;
const call=`${baseUrl}/call/`;

export default {
    multiVendor,
    customers,
    call,
};